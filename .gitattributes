# Shell-Skripte MUESSEN Unix-Zeilenenden behalten, sonst scheitert Linux mit
# "bad interpreter: /usr/bin/env sh^M". Gilt unabhaengig von core.autocrlf.
*.sh text eol=lf
# Windows-Skripte umgekehrt
*.bat text eol=crlf
*.ps1 text eol=crlf
